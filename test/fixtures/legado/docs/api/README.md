# API

Documentação da API pública. Este arquivo disputa o nome `README` com o da
raiz do projeto: é o caso que o regime descoberto resolve qualificando o id
em vez de descartar o documento.

Veja [endpoints](endpoints.md).
